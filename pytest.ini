[tool:pytest]
markers =
    azure_required: marks tests that require Azure credentials/connectivity
    integration: marks tests that require full environment setup
    unit: marks pure unit tests that don't require external dependencies

# Filter out noisy warnings
filterwarnings =
    ignore::pydantic.warnings.PydanticDeprecatedSince20
    ignore::DeprecationWarning:pydantic.*